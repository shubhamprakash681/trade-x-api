server:
  port: ${NOTIFICATION_SERVICE_PORT:8085}
  shutdown: graceful

spring:
  datasource:
    url: ${NOTIFICATION_DB_URL:jdbc:postgresql://postgres:5432/${POSTGRES_DB:tradex}}
    username: ${NOTIFICATION_DB_USERNAME:${POSTGRES_USER}}
    password: ${NOTIFICATION_DB_PASSWORD:${POSTGRES_PASSWORD}}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

springdoc:
  swagger-ui:
    enabled: false
