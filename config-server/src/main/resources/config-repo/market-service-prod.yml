server:
  port: ${MARKET_SERVICE_PORT:8082}
  shutdown: graceful

spring:
  datasource:
    url: ${MARKET_DB_URL:jdbc:postgresql://postgres:5432/${POSTGRES_DB:tradex}}
    username: ${MARKET_DB_USERNAME:${POSTGRES_USER}}
    password: ${MARKET_DB_PASSWORD:${POSTGRES_PASSWORD}}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

tradex:
  market:
    topic: ${TRADEX_PRICE_TOPIC:tradex.market.prices}
    history:
      years: ${TRADEX_MARKET_HISTORY_YEARS:10}
      interval: ${TRADEX_MARKET_HISTORY_INTERVAL:SECONDS}
      generation-delay-ms: ${TRADEX_MARKET_GENERATION_DELAY_MS:1000}

springdoc:
  swagger-ui:
    enabled: false
